package com.example.composestarter

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * 本地 JVM 单元测试，用于验证测试环境已正确配置。
 */
class ExampleUnitTest {

    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
